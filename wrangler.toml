name = "portal-4sup"
compatibility_date = "2024-01-01"

# Pages Functions (advanced mode)
pages_build_output_dir = "."

# Binding do D1 (será preenchido depois)
[[d1_databases]]
binding = "DB"
database_name = "docs_portal"
database_id = "" # preencher após criar o D1 no painel
